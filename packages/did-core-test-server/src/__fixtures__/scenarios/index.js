const resolve = require("./resolve");
const did_syntax = require("./did-syntax");
const unknown_scenario = require("./unknown-scenario");
const all = require("./all");

module.exports = { resolve, did_syntax, unknown_scenario, all };
